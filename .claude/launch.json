{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "roundtable",
      "runtimeExecutable": ".venv/bin/uvicorn",
      "runtimeArgs": ["backend.main:app", "--host", "0.0.0.0", "--port", "8000", "--env-file", ".env"],
      "port": 8000
    }
  ]
}
